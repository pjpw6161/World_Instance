{
  "name": "@world-forge/wasm-engine",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@world-forge/shared": "0.0.0"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vitest": "^4.0.16"
  }
}
